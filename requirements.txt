python-telegram-bot==21.4
openpyxl==3.1.5
SQLAlchemy==2.0.31
python-dotenv==1.0.0

# AI-слой (packages/smeta_ai). Импортируется лениво и только при заданном
# OPENAI_API_KEY: без ключа проект поднимается на стабе и без этого пакета.
openai==2.52.0

# PDF (packages/smeta_export). Чистый Python, без системных зависимостей;
# шрифт лежит в репозитории, а не берётся из системы (ADR-021).
reportlab==5.0.0

# API (apps/api). Боту не нужны, но ставятся одной командой.
fastapi==0.141.1
uvicorn[standard]==0.52.0
pydantic==2.13.4
