"""apps/share — публичная страница сметы по ссылке.

Отдельное приложение, а не маршрут в apps/api. Причина в ADR-008: тот API не
касается базы вовсе, и это его главное свойство — персональных данных в нём
нет по построению, публиковать его можно не думая. Публичная страница базу
читает, значит правило пришлось бы не уточнить, а отменить. Вместо этого —
второй сервис со своими правилами, и оба остались целыми (ADR-020).
"""
